# Transient QEMU run / debug artifacts (regenerated by
# scripts/linux000-run.* and qemu-run.*). Not committed.
qemu_serial.txt
qemu.log
qemu_stderr.txt
*.ppm
_qmp_shot.py
# Keep committed proof artifacts (e.g. linux000_screen.png) — they
# are intentionally tracked, so no broad *.png ignore here.
